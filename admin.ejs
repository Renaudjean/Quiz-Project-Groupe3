<!DOCTYPE html>
<html lang="en">
<head>
    <%- include ('page/templates/head')  %> 
    <title>QUIZZDOM | Admin</title>
</head>
<body>
    <%- include('page/templates/header-shown') %>
    <main>
        <div class="container">
            <div class="fond">
                <div class="cont_add-new_quiz">
                    <h1 class="h1">Hello, Amanda123!</h1>
                    <a href="#" class="btn-primary">+ Add new quiz</a>
                </div>
                <p class="p_admin">Quizes you’ve already added:</p>
                <div class="flex-quiz">
                    <% for(q of quiz) { %> 
                        <div class="quizz">
                            <img src="<%= q.Quiz_Photo %>" alt="Quiz <%= q.Quiz_Name %>" class="img-quiz">
                            <div class="quizz_content">
                                <h2 class="h2-quiz"><%= q.Quiz_Name %></h2>
                                <p class="p-quiz"><%= q.Quiz_Description%></p>
                                <a href="quiz/<%= q.Quiz_ID%>" class="btn-secondary edit">Edit</a>
                                <a href="quiz/<%= q.Quiz_ID%>" class="btn-secondary delete">Delete</a>
                        </div>
                    </div> 
                    <% } %> 
                </div>
            </div>
        </div>
    </main>
    <scrip></script>
</body>
</html>